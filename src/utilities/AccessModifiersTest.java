package utilities;

import day40_FinalKeyword.ProtectedAccessModifier;

public class AccessModifiersTest {

    public static void main(String[] args) {

     //   System.out.println(ProtectedAccessModifier.name1); // default is NEVER visible outside the package
     //   System.out.println(ProtectedAccessModifier.name2); // protected is NOT ALWAYS visible outside the package, but sometimes can be visible(inheritance)

    }

}
